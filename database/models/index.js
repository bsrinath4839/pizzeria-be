const IngredientModel = require('./ingredient.model');
const PizzaModel = require('./pizza.model');
const CartModel = require('./cart.model');
const ToppingsModel = require('./topping.model');

module.exports = {
    CartModel,
    IngredientModel,
    PizzaModel,
    ToppingsModel,
}